/**
 * Сценарий 3: "Колобок" (Graph Cycle Detection)
 *
 * Катится Колобок по дорожке, а навстречу ему — то Заяц, то Волк,
 * то Медведь. Лес густой, тропинки путаные. Каждое знакомство ведет к новым
 * тропинкам. Беда случится, если тропинка выведет Колобка к тому, кого он
 * уже встречал в рамках этого же путешествия — так наш герой попадет в
 * бесконечную петлю встреч и никогда не докатится до финала.
 * Нужно вычислить, не зациклился ли путь.
 */

export type ForestGraph = Record<string, string[]>;

export function hasForestCycle(graph: ForestGraph): boolean {
  // Вычисли, не попал ли Колобок в бесконечный цикл
  return false;
}
